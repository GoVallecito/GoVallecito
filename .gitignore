# Dependencies / tooling
node_modules/
.wrangler/

# Wrangler local secrets — NEVER commit
.dev.vars
.dev.vars.*

# Claude Code personal/local settings (not shared)
.claude/settings.local.json

# Free-image ORIGINALS (8–22 MB each) — reproducible via scripts/fetch-free-images.sh;
# never deploy or commit. Only the optimized web copies in public/assets/img/ ship.
assets-originals/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
